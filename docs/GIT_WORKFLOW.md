# Git Workflow для совместной работы

## 🌿 Структура веток

Проект использует следующую структуру веток:

- **`main`** - основная ветка с актуальной версией проекта
- **`gleb`** - рабочая ветка для Глеба
- **`ilya`** - рабочая ветка для Ильи
- **`leonid`** - рабочая ветка для Леонида

## 🚀 Начало работы

### Первая настройка (один раз)

1. **Клонировать репозиторий** (если еще не клонировали):
```bash
git clone <url_репозитория>
cd neural_network
```

2. **Получить все ветки**:
```bash
git fetch origin
```

3. **Переключиться на свою ветку**:
```bash
# Для Глеба:
git checkout gleb

# Для Ильи:
git checkout ilya

# Для Леонида:
git checkout leonid
```

## 📝 Ежедневная работа

### Перед началом работы

1. **Обновить свою ветку с main**:
```bash
# Переключиться на main
git checkout main

# Получить последние изменения
git pull origin main

# Вернуться на свою ветку
git checkout <ваша_ветка>  # gleb, ilya или leonid

# Слить изменения из main в свою ветку
git merge main
```

### Во время работы

1. **Делать коммиты регулярно**:
```bash
# Посмотреть изменения
git status

# Добавить файлы
git add <файлы>

# Сделать коммит с понятным сообщением
git commit -m "Описание изменений: что сделано и зачем"
```

**Примеры хороших сообщений коммитов:**
- `"Этап 1: Добавлен анализ распределения целевой переменной"`
- `"Этап 2: Реализована обработка пропусков методом KNN imputation"`
- `"Этап 3: Добавлен отбор признаков методом Lasso"`

### После завершения работы

1. **Отправить изменения в удаленный репозиторий**:
```bash
git push origin <ваша_ветка>
```

## 🔀 Слияние изменений в main

Когда этап работы завершен и проверен:

1. **Убедиться, что все изменения закоммичены**:
```bash
git status
```

2. **Обновить свою ветку с main**:
```bash
git checkout main
git pull origin main
git checkout <ваша_ветка>
git merge main
```

3. **Решить конфликты** (если есть):
```bash
# Git покажет файлы с конфликтами
# Отредактировать файлы, разрешить конфликты
git add <файлы_с_разрешенными_конфликтами>
git commit -m "Разрешение конфликтов"
```

4. **Создать Pull Request (если используете GitHub/GitLab)** или **слить в main**:
```bash
# Переключиться на main
git checkout main

# Слить свою ветку
git merge <ваша_ветка>

# Отправить в удаленный репозиторий
git push origin main
```

5. **Обновить свою рабочую ветку**:
```bash
git checkout <ваша_ветка>
git merge main
```

## ⚠️ Важные правила

### ✅ Делайте так:

- **Регулярно коммитить** - не накапливайте много изменений
- **Писать понятные сообщения коммитов** - что сделано и зачем
- **Обновляться с main перед началом работы** - чтобы быть в курсе изменений
- **Тестировать перед коммитом** - убедиться, что код работает
- **Работать в своей ветке** - не коммитить напрямую в main

### ❌ Не делайте так:

- **Не коммитить в main напрямую** - используйте свою ветку
- **Не игнорировать конфликты** - решайте их сразу
- **Не делать больших коммитов** - разбивайте на логические части
- **Не коммитить временные файлы** - используйте .gitignore

## 🔧 Полезные команды

### Просмотр истории
```bash
# История коммитов
git log --oneline --graph --all

# История конкретного файла
git log -- <файл>

# Показать изменения в файле
git diff <файл>
```

### Отмена изменений
```bash
# Отменить изменения в файле (до добавления в staging)
git checkout -- <файл>

# Убрать файл из staging (но сохранить изменения)
git reset HEAD <файл>

# Отменить последний коммит (сохранить изменения)
git reset --soft HEAD~1
```

### Работа с ветками
```bash
# Посмотреть все ветки
git branch -a

# Создать новую ветку
git checkout -b <новая_ветка>

# Удалить ветку (локально)
git branch -d <ветка>

# Удалить ветку (в удаленном репозитории)
git push origin --delete <ветка>
```

## 📋 Чек-лист перед коммитом

- [ ] Код работает и протестирован
- [ ] Документация обновлена (если нужно)
- [ ] Сообщение коммита понятное и информативное
- [ ] Не добавлены временные файлы
- [ ] Изменения логически связаны (один коммит = одна задача)

## 🤝 Распределение этапов

Рекомендуется распределить этапы между участниками:

**Вариант 1: Последовательная работа**
- Каждый участник работает над своим этапом
- После завершения этапа, изменения сливаются в main
- Следующий участник обновляется и начинает свой этап

**Вариант 2: Параллельная работа**
- Участники работают над разными этапами параллельно
- Регулярно синхронизируются через main
- Внимательно разрешают конфликты при слиянии

## 📞 Если возникли проблемы

1. **Конфликты при слиянии:**
   - Git покажет файлы с конфликтами
   - Откройте файлы, найдите маркеры `<<<<<<<`, `=======`, `>>>>>>>`
   - Выберите нужный код или объедините изменения
   - Сохраните файл и сделайте `git add <файл>`

2. **Потеряли изменения:**
   - `git reflog` - покажет историю всех действий
   - `git checkout <commit_hash>` - вернуться к нужному коммиту

3. **Нужна помощь:**
   - Обсудите с командой
   - Проверьте документацию Git
   - Используйте `git help <команда>` для справки

---

*Обновляйте этот документ по мере необходимости!*

